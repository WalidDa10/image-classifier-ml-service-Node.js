import React from "react";
import UploadForm from "./components/uplodForm";

function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 to-blue-200">
      <UploadForm />
    </div>
    
  );
}

export default App;
